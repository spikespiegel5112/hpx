import { connect } from 'react-redux'

import ForgetPwd from './ForgetPwd'

const mapActionCreators = {

}

const mapStateToProps = (state) => ({

})

export default connect(mapStateToProps, mapActionCreators)(ForgetPwd)